<h4>There are <%= @libraries.size %> libraries listed.</h4>

<% @libraries.each do |library| %>
  <div>
    <h3><%= library.name %></h3>
    <% if current_user %>
      <%= button_to "Join", library_user_path(library) %>
    <% end %>
  </div>
<% end %>
